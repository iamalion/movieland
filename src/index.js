import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// The render() method takes two arguments, the first being the component to render, and the second being the DOM element to render it to. In this case, we're rendering the App component to the element with the id of root.